services:
  discord-watchdog:
    build: .
    container_name: discord-watchdog
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
